spring:
  application:
    name: movie-service

  datasource:
    url: jdbc:mysql://localhost:3306/movieservicedb
    username: root
    password: Mani@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false

server:
  port: 8081
#  servlet:
#    context-path: /api

logging:
  level:
    com.deloitte: DEBUG
    org.springframework.web: INFO
    org.hibernate: INFO